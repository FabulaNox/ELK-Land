# Runtime data directories
data/

# Terraform state and runtime files
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
*.tfstate.*

# Personal configuration files
terraform.tfvars
local.tfvars
.context.md
SECURITY_AUDIT.md
DEPLOYMENT_CONTEXT.md

# SSL certificates (planned)
*.pem
*.key
*.crt
*.p12
*.pfx

# Environment files
.env
.env.*

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Beats and monitoring configuration (generated/runtime files)
beats-config/
elastic-agent*/
*.pid
*.lock

# Working project files - Beat configurations
*beat.yml
*beat.yaml
metricbeat.yml
filebeat.yml
packetbeat.yml
heartbeat.yml
auditbeat.yml
winlogbeat.yml
functionbeat.yml

# Docker Compose files for monitoring stack
docker-compose*.yml
docker-compose*.yaml

# Deployment scripts (auto-generated)
deploy-*.sh
setup-*.sh
install-*.sh

# Fleet and Elastic Agent runtime
fleet-server*/
elastic-agent-*/
agent.yml
agent.yaml

# Beats data and registry files
*.dat
registry/
.filebeat/
.metricbeat/
.packetbeat/

# Deployment results and container artifacts  
# No deployment artifacts, runtime data, or container state should be in repository
# This ensures clean, portable, and secure deployments across environments
